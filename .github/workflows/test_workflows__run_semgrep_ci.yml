name: Test workflows/run_semgrep_ci

on:
  workflow_dispatch: # Allows you to run this workflow manually from the Actions tab
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # default:
  #   uses: ./.github/workflows/run_semgrep_ci.yml

  #
  # # TODO? or XXX?
  # python__matrix:
  #   strategy:
  #     fail-fast: false
  #     max-parallel: 6
  #     matrix:
  #       python-version:
  #         - "3.13"
  #         # - "3.14"

  #   name: python @ ${{ matrix.python-version }}
  #   uses: ./.github/workflows/run_semgrep_ci.yml
  #   with:
  #     python-version: ${{ matrix.python-version }}

  #
  config__matrix:
    strategy:
      fail-fast: false
      max-parallel: 6
      matrix:
        config:
          - "p/default"

    name: "config: '${{ matrix.config }}'"
    uses: ./.github/workflows/run_semgrep_ci.yml
    with:
      config: ${{ matrix.config }}
